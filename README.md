# Iterator
Generated by [Rojo](https://github.com/rojo-rbx/rojo) 7.4.1.

Iterator is a roblox module used to create lazy iterators. (inspired by rustlang iterators)

## API
`Iterator` is documented and type-annotated. Documentation can be found in the module above each function.

## Build from Scratch
To build the place from scratch, use:

```bash
rojo build -o "Iterator.rbxlx"
```

Next, open `Iterator.rbxlx` in Roblox Studio and start the Rojo server:

```bash
rojo serve
```

For more help, check out [the Rojo documentation](https://rojo.space/docs).